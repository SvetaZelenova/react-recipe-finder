import { Fragment } from "react";
import SearchComponent from "../components/Search";

const HomePage = () => {
  return (
    <Fragment>
      <SearchComponent />
    </Fragment>
  );
};

export default HomePage;
